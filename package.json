{
  "name": "@procedural-human/root",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "pnpm build:contracts && pnpm --filter @procedural-human/web build",
    "build:contracts": "pnpm --filter @procedural-human/core build && pnpm --filter @procedural-human/units build && pnpm --filter @procedural-human/math build && pnpm --filter @procedural-human/anatomy build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "schema:validate": "node tools/validate-schemas.mjs",
    "schema:validate:python": "uv run --package ph_validation python authoring/python/ph_validation/src/ph_validation/shared_schema_validation.py",
    "schema:validate:shared": "pnpm schema:validate && pnpm schema:validate:python",
    "test": "pnpm build:contracts && pnpm schema:validate:shared && node --test tools/*.test.mjs tests/*.test.mjs",
    "typecheck": "pnpm --filter @procedural-human/core typecheck && pnpm --filter @procedural-human/units typecheck && pnpm --filter @procedural-human/math typecheck && pnpm --filter @procedural-human/anatomy typecheck && pnpm --filter @procedural-human/web typecheck",
    "check:boundaries": "node tools/check-package-boundaries.mjs"
  },
  "devDependencies": {
    "@eslint/js": "9.34.0",
    "ajv": "8.17.1",
    "eslint": "9.34.0",
    "globals": "16.3.0",
    "prettier": "3.6.2",
    "typescript": "5.9.2",
    "typescript-eslint": "8.40.0"
  }
}
